# 项目复盘

这份文档**留给你自己写**。代码大部分是 AI 帮你敲的，但「你学到了什么」只有你自己知道。写完它，这个项目才算真的结束。

下面的问题不用全答，挑有感觉的写。

## 1. 这个项目里，我第一次真正搞懂了什么？

不是「我知道了 git commit 是提交」这种复述，而是**之前一直模糊、现在清楚**的东西。

可能的素材：为什么 `git add` 和 `git commit` 是两步？为什么改完代码要立刻跑一次？「本地领先远程 N 个提交」是什么意思？

（写在这里）

## 2. 哪里卡我最久？最后怎么解决的？

卡住的地方才是真正学到东西的地方。把整个过程写下来，下次遇到同类问题你会有感觉。

可能的素材：

- `dubious ownership` 那次（仓库所有者对不上）
- PowerShell 执行策略拦 npm 那次
- 端口被旧进程占用、接口返回 404 那次
- 提交信息里的转义引号把参数解析搞坏那次
- `.pytest_cache` 的权限问题

（写在这里）

## 3. 我用 Codex 的方式有什么问题？

回到 `docs/PLAN.md` 第 7 节「Codex 使用手册」，那里列了四个危险信号。有没有中招？

- 有没有连续几次直接复制粘贴、没读代码？
- 有没有自己没试就先问？
- 能不能说清楚 `game.py` 里每个函数在干什么？

（写在这里）

## 4. 如果能重来，我会怎么做得不一样？

比如「早点写 .gitignore」「一个提交只做一件事」「先跑起来再加功能」。

（写在这里）

## 5. 下一步想做什么？

看 `IDEAS.md` 里的想法清单。我最推荐先做 **GitHub Actions 自动跑测试的徽章**——你已经有了测试、有了流水线，把它变成 README 上那个小绿标就很有成就感。

（写在这里）

## 附：这个项目的数字

跑这几条命令自己填：

```powershell
git rev-list --count HEAD                                   # 提交数
(git ls-files | Measure-Object -Line).Lines                 # 文件数
(Get-ChildItem -Recurse -Include *.py,*.js,*.css,*.html | Get-Content | Measure-Object -Line).Lines
```

| 项目 | 数字 |
| --- | --- |
| 提交数 | |
| 测试数 | Python 42 条（24 + 18）+ JavaScript 40 条 = 82 条 |
| 代码行数 | |
| 花了多少天 | |
